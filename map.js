const firstNames = ['영훈', '유누','동욱','태호'];
const lastNames=['강', '이','순','성'];

firstNames.map((firstName,i)=>{
  return lastName[i] + firstName;

}); /*firstName.map(firstName, i)=>lastnames[i]+firstName}; */
console.log(fullNames);
